# 版本记录

## 2026-07-12 当前自定义版本

本提交在上游原始 `zhuzhige123/obsidian-weave-reader` 快照基础上，覆盖为当前本地 `weave-reader` 插件版本。

主要变化包括：

- 去除原始版本中与当前使用目标不一致的部分运行逻辑。
- 保留并同步当前 Obsidian 插件可运行入口文件 `manifest.json`、`main.js`、`styles.css`。
- 同步当前 `src` 源码结构与本地修改。
- 加入三种阅读界面模式、语义标注方案、标注存储与书籍数据结构相关修改。
- 包含当前已实现的语义配置、书籍标注读取、切换方案清理标注等修复。

未提交内容：

- `data.json`：包含本机插件设置、授权/激活信息、设备指纹等个人数据，已排除。
- `node_modules/`：依赖目录，应通过 `npm install` 重新安装。
- `cache/`、`state/`、`backups/`、`dist/`：本地运行缓存、状态、备份或构建输出，不作为源码提交。
